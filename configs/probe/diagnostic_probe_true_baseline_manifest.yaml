# Validator-side manifest for diagnostic probe (true_baseline).
# Schema differs from the runner config -- this is the
# {sweeps: [...]} shape consumed by validate_paper_claims's
# manifest CLI mode (Phase 2 prompt 1).
sweeps:
  - name: diagnostic_probe_true_baseline
    csv: ../../logs/probe/diagnostic/true_baseline/results.csv
    max_invalid_fraction: 0.05
