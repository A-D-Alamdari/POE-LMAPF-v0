# PAUSED — read before resuming

Project is in an audited pause state.  Read
[`reports/audit/RESUME_DECISION.md`](reports/audit/RESUME_DECISION.md)
**before touching anything**, especially before regenerating any
sweep.  The experimental section requires a re-run; the verdict
section explains why and what setup decision must precede the
re-run so the compute is not wasted.

Audit series committed under `reports/audit/` (steps 00-11).
